# HTTP port used by the NestJS API and the production application.
PORT=3000

# Pino log level: trace, debug, info, warn, error, fatal, or silent.
LOG_LEVEL=info

# Relative paths are resolved from the repository root. Do not point this at a
# shared or production database when running local development commands.
DATABASE_PATH=data/roomly.sqlite
